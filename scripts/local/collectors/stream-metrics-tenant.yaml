apiVersion: jetstream.nats.io/v1beta2
kind: Stream
metadata:
  name: metrics-tenant
  namespace: collectors
spec:
  name: METRICS_TENANT
  subjects:
    # metrics.tenant.<tenant-id>
    - metrics.tenant.*
  storage: file
  retention: limits
  maxAge: 4h
  replicas: 3
