apiVersion: jetstream.nats.io/v1beta2
kind: Stream
metadata:
  name: logs-input
  namespace: collectors
spec:
  name: LOGS_INPUT
  subjects:
    - logs.input
  storage: file
  retention: limits
  maxAge: 4h
  replicas: 3
